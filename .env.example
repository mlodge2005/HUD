DATABASE_URL="postgresql://user:password@localhost:5432/hud?schema=public"
SESSION_SECRET="change-me-to-a-long-random-string"
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=admin123
SEED_ADMIN_DISPLAY_NAME=Admin
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
GOOGLE_MAPS_API_KEY=
# For map widget in browser (optional): set in .env as NEXT_PUBLIC_GOOGLE_MAPS_API_KEY

# Google Calendar OAuth (per-user in Settings; HUD shows active streamer's calendar)
# Redirect URI is computed from request origin (http(s)://host/api/google/oauth/callback).
# In Google Console, add Authorized redirect URIs for each environment, e.g.:
#   http://localhost:3000/api/google/oauth/callback
#   https://<your-domain>/api/google/oauth/callback
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# Ably (realtime chat + presence). Server uses ABLY_API_KEY for token auth.
ABLY_API_KEY=
